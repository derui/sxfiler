(jbuild_version 1)

(alias
 ((name runtest)
  (deps (test_sxfiler_completer.bc.js))))

(executable
 ((name test_sxfiler_completer)
  (libraries (js_of_ocaml sxfiler.completer mocha_of_ocaml))
  (js_of_ocaml ((flags ("--pretty" "--source-map-inline"))))
  (preprocess (pps (js_of_ocaml-ppx)))))
