(jbuild_version 1)

(alias
 ((name runtest)
  (deps (test_sxfiler_common.bc.js))))

(executable
 ((name test_sxfiler_common)
  (libraries (js_of_ocaml sxfiler.common))
  (js_of_ocaml ((flags ("--pretty" "--source-map-inline"))))
  (preprocess (pps (js_of_ocaml-ppx)))))
