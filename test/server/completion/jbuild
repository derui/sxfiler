(jbuild_version 1)

(alias
 ((name runtest)
  (deps (test_sxfiler_server_completion.exe dict_file.txt))
  (action (run ${<}))))

(executable
 ((name test_sxfiler_server_completion)
  (flags (-safe-string))
  (libraries (alcotest sxfiler.server.completion migemocaml))))
