(jbuild_version 1)

(library
 ((name sxfiler_common)
  (public_name sxfiler.common)
  (libraries (js_of_ocaml
              unix
              sxfiler_kbd
              jsoo_node
              minimal-monadic-caml
              mocha_of_ocaml))
  (preprocess (pps (js_of_ocaml-ppx)))))
