#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#

;; init form
(progn
  (ros:ensure-asdf)
  (asdf:load-asd (truename  "./sxfiler.asd"))
  (ql:quickload '(:sxfiler :rove) :silent t))

(defpackage :ros.script.commonlisp.3732218566
  (:use #:cl))
(in-package :ros.script.commonlisp.3732218566)

;; Run test with using asdf
(defun main (&rest argv)
  (declare (ignorable argv))
  (asdf:test-system :sxfiler))
;;; vim: set ft=lisp lisp:
